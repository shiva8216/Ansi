- name: variables
  hosts: local
  connection: local
  vars: # PLAY level variables, global variables
    COURSE: "DevOps and linux"
    DURATION: 120hRS
    TRAINER: Shiva
  tasks:
  - name: Print the variable
    vars: #Task level variables,accesible only in the task .Task variabgles can override play level variables.Local variables
    COURSE: Ansible
    ansible.builtin.debug:
      msg:"Course is {{ COURSE }}, Duration is {{ DURATION }}, Trainer is {{ TRAINER }}"

  - name: print the variable1
    ansible.builtin.debug:
     msg:"Course is {{ COURSE }}, Duration is {{ DURATION }}, Trainer is {{ TRAINER }}"