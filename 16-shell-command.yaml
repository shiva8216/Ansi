- name: loops
  hosts: run
  #connection: local
  tasks:
    - name: Print names
      ansible.builtin.debug:
         msg: "Hello {{ item }}"

      loop:
      - shivudu
      - chinnu
      - kannaya
